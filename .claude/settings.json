{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -q '\\.rs'; then cargo clippy --quiet -- -D warnings 2>&1 | head -20; fi"
          }
        ]
      }
    ]
  }
}
